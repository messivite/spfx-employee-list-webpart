const { PublicClientApplication } = require("@azure/msal-node");

async function run() {
    // We cannot easily do this without user interaction because the user logs in via browser.
    // Instead we will just edit EmployeeGrid to console.log different endpoints or do it from there.
}
run();
